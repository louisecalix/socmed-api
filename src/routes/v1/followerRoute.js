// import { Router } from "express";
// import authorization from "../../middlewares/authorization.js";
// import authentication from "../../middlewares/authentication.js";
// import FollowersController from "../../controllers/v1/follower_controller.js";

// const followerRouter = new Router();
// const follower = new FollowersController();

// followerRouter.use (authorization);

// followerRouter.post('/follow/:following_id', authentication, follower.followUser.bind(follower));
// followerRouter.delete('/unfollow/:following_id', authentication, follower.unfollowUser.bind(follower));
// followerRouter.get('/profile/followers', authentication, follower.getUserFollowers.bind(follower));
// followerRouter.get('/profile', authentication, follower.getUserFollowers.bind(follower));
// followerRouter.get('/profile/followings', authentication, follower.getUserFollowings.bind(follower));


// export default followerRouter;

//FOLLOW IN PROFILE
